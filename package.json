{
  "name": "exercise-one",
  "version": "1.0.0",
  "engines": {
    "node": "^14.0.0"
  },
  "description": "",
  "scripts": {
    "setup": "npm install && cd server && npm install && cd ../client && npm install && cd ..",
    "watch": "concurrently \"cd server && npm run watch\" \"cd client && npm run start\""
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^7.0.0"
  },
  "dependencies": {}
}
